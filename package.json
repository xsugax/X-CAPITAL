{
  "name": "x-capital-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "@prisma/client": "^5.10.2",
    "prisma": "^5.10.2"
  }
}
